version: 2

models:
  - name: stg_orders
    description: one order per row
    columns:
      - name: orderid
        description: The primary key for stg_orders
      - name: shipmode
        description: '{{doc("shipmode")}}'
